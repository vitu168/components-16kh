name: sdkcomponents16
description: A Flutter package providing a dynamic multi-select dropdown component with search, async data loading, and form validation support.
version: 0.0.1
homepage: https://github.com/vitu168/components-16kh
repository: https://github.com/vitu168/components-16kh
issue_tracker: https://github.com/vitu168/components-16kh/issues

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - widget
  - dropdown
  - multi-select
  - form

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: