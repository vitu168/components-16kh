name: dynamic_multi_dropdown
description: A Flutter package providing a dynamic multi-select dropdown component with search, async data loading, and form validation support.
version: 0.0.3
homepage: https://github.com/vitu168/dynamic_multi_dropdown
repository: https://github.com/vitu168/dynamic_multi_dropdown
issue_tracker: https://github.com/vitu168/dynamic_multi_dropdown

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - widget
  - dropdown
  - multi-select
  - form

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: